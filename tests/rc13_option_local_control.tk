import core/option::{Option}

shape Range(start: usize, end: usize)

fn main() -> i32 {
    auto range = Option<Range>::Some(Range(start = 1:usize, end = 2:usize))
    return range.unwrap().end as i32
}

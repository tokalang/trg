# Universal Search Matrix Specification

This document details the search and perception contract for `trg`.

## Core Features
- Literal fast-path search (`-F`)
- Regex pattern matching (`-E`)
- Case modes: sensitive (`-s`), ignore (`-i`), smart-case (`-S`)

### Code Example
```python
def example():
    return "matrix_ok"
```

End of document.

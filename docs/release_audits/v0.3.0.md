# trg v0.3.0 Release Candidate Audit Report

- **Package**: `trg`
- **Candidate Commit**: `51491f03bf0dceaabdab9e5155d21f33bf49e986`
- **Audited Target Version**: `0.3.0`
- **Audit Date**: `2026-09-02T03:32:00Z`
- **Host Platform**: `macOS arm64 (Darwin 25.5.0 arm64, Apple clang 21.0.0)`
- **Toolchain**: `toka 1.0.0-rc.8`, `tokac 1.0.0-rc.8`, `compiler = "1.0.0-rc.11"`
- **Audit Status**: **GO (Release Candidate Qualified on macOS arm64)**

---

## 1. Executive Summary & Defect Ledger

All mandatory release gates, supply chain verifications, clean consumer replays, 49 qualification tests, differential prefilter parity suites, resource boundary limits, and performance regression benchmarks have been audited and passed.

| Severity | Count | Status | Notes |
| :--- | :---: | :---: | :--- |
| **P0 (Blocker)** | **0** | **CLEARED** | Supply chain, registry lock, offline replay, and error precedence all verified. |
| **P1 (Critical)** | **0** | **CLEARED** | `-w` punctuation boundary fixed; release identities unified to 0.3.0. |
| **P2 (Minor)** | **1** | **RECORDED** | Human/Count modes construct `MatchRange` vector; `is_match` fast-path queued for Phase 2. |

---

## 2. Supply Chain & Public Registry Integrity

The full dependency supply chain has been verified end-to-end:

```text
GitHub Tag: tokalang/regex@v0.3.0 (Commit 02114477c4cd1bdb1c9554f200fcc31bf79ab52f)
  │
  ├──► GitHub Release Asset: regex-0.3.0.tar.gz
  │     SHA-256: cf365755da461ff9bb48df211aca8ddbcf2c630cc1caedd8395b5200a8d8beb9 (Matches byte-for-byte)
  │
  ├──► Public Static Catalog: https://pkg.tokalang.dev/catalog.json (Merged PR tokalang/toka-registry#11)
  │     Package: regex @ 0.3.0, installable: true, latest_version: 0.3.0
  │
  ├──► Local trg Manifest & Lock: trg/package.tk & trg/package.lock
  │     Lock Entry: package regex registry regex 0.3.0 cf365755... c0a8b503... -
  │
  └──► Extracted Source Tree Hash:
        Tree SHA-256: c0a8b50305ae4e6e7ee82c1505ca314dd0bc98968d088d5e631dc39104db68a1 (Matches lock)
```

- **Tarball Security Inspection**: Verified 11 archive entries contain no `.git` directories, no precompiled binary artifacts (`.o`, `.a`, `.dylib`), and no relative path escapes (`..`).

---

## 3. Clean Public Consumer Replay (Online & Offline)

An isolated clean project referencing `regex = "regex:0.3.0"` was executed in an isolated temporary sandbox:

1. **Online Stage (Empty Cache & Default Registry `https://pkg.tokalang.dev`)**:
   - `toka fetch`: Successfully resolved 1 package, downloaded archive, generated compliant `package.lock`.
   - `toka build`: Successfully compiled `clean_consumer`.
   - Execution: Ran and passed all regex assertions (including punctuation `-w` boundary `.-.` vs `a-b`).
2. **Offline Stage (Purged Packages & Unreachable Dead Registry `http://127.0.0.1:9`)**:
   - Unpacked `.toka/packages/` and `target/` directories were completely purged; only `.toka/cache/archives/` and `package.lock` retained.
   - `TOKA_OFFLINE=1 toka fetch`: Successfully verified lock with zero lock drift.
   - `TOKA_OFFLINE=1 toka build`: Extracted from archive cache and compiled offline.
   - Execution: Ran and exited `0`.

---

## 4. Package-Level Qualification (`trg`)

- **`toka doctor`**: Standard library, compiler, formatter, and native linker driver OK.
- **`toka check --json package.tk`**: Diagnostics clean (`success: true`).
- **`toka build`**: Successfully built `target/debug/trg`.
- **49 Qualification Tests (`tests/qualify.py`)**:
  - Primary test subject: `target/debug/trg` (package artifact).
  - Supplementary gate: `target/trg` (direct `tokac` binary).
  - Version identity on both: `trg 0.3.0 (Toka)`.
  - Compiler mappings verify dependency links `.toka/packages/regex-0.3.0/lib/official/regex.tk`.
  - Result: **49 / 49 PASS (100%)**.

---

## 5. Regex & Prefilter Correctness Deep-Dive (Zero False Negatives)

Evaluated 864 differential configurations across multi-shape corpora (standard, empty lines, CRLF, non-EOL, zero-width, multiple matches) comparing `--no-prefilter` vs default prefilter:

- **Tested Pattern Shapes**: `foo|bar`, `(foo|foobar)x`, `foo(bar)?`, `a*`, `.*`, `^$`, `^|a`, `[a-z]+`, `(ab){0,3}`, `(ab){1,3}`, `(fn|pub fn)\s+[A-Z_]+`, `-`.
- **Flag Combinations**: `-i`, `-w`, `-x`, `-v`, `-l`, `-c`, `-n -C 2`, `-n -B 1 -A 2`, `--json`, and mixed flags.
- **Invariant Audit**: Directly verified `can_match_empty == true` $\implies$ `required_literal == None`.
- **Differential Result**: **864 / 864 configurations produced 100% byte-for-byte identical stdout, stderr, exit codes, and unbroken JSON event framing.**

---

## 6. Resource, Safety & Error Boundaries

- **1MB Line Memory Limit**: Exactly 1,048,576 bytes text accepted (exit 0); 1,048,577 bytes text rejected fail-closed (exit 2).
- **64MiB BeforeRing Memory Limit**: Cumulatively bounded, fails fast with exit 2 on overflow.
- **Parser Security Limits**: Patterns >4096 bytes rejected (exit 2); group nesting depth >128 rejected (exit 2).
- **Adversarial Non-Backtracking NFA**: Executed `(a?)^25 a^25` against `a^25` in **4.0 ms** (zero exponential backtracking).
- **Symlink Cycle Safety**: POSIX `readlink` skips directory loops without hanging or recursion.
- **Ripgrep Error Precedence**: Match found + unreadable path $\implies$ overall exit code `2`.
- **Early Regex Compile Error**: Fails fast with exit 2 before directory traversal occurs.
- **JSON Framing**: Unbroken framing (`begin → end → summary`) maintained across empty, binary, overlong, and I/O error files.
- **Broken Pipe Handling**: `SIGPIPE` ignored, early graceful termination with exit 0 on closed pipe (`| head -n 1`).

---

## 7. Performance Benchmarks

Benchmarked against fixed dataset SHA `f87f8d95144ba99152573e3ac672f4d51b5e24aa5d134abcaa1375c4aa3f1db5` (21 iterations, median reported):

| Scenario | `trg v0.3.0` Median | `rg -j1` Baseline | Speedup / Parity |
| :--- | :---: | :---: | :---: |
| **Literal Default (`pub fn`)** | **3.680 ms** | 3.679 ms | **1.00x (0% regression vs literal baseline)** |
| **Literal Fixed (`-F`)** | **3.633 ms** | - | Fast fixed-string path |
| **Literal Ignore-Case (`-i`)** | **3.466 ms** | - | Full ASCII folding |
| **Literal Word Boundary (`-w`)** | **3.487 ms** | - | Left/Right ASCII word guard |
| **Context Streaming (`-C 2`)** | **3.424 ms** | - | Merged context window |
| **Regex with Prefilter (`-E`)** | **3.682 ms** | 3.905 ms | **2.11x faster than without prefilter** |
| **Regex without Prefilter** | **7.783 ms** | - | Raw Thompson NFA execution |

---

## 8. Platform Matrix & Scope Declaration

- **Audited & Qualified Platform**: `macOS arm64 (Darwin 25.5.0)`
- **Cross-Platform Manifest Declaration**: `package.tk` declares `targets = ["macos", "linux"]`. All POSIX interfaces (`readlink`, `fread`, `fseeko`, `errno`) use platform-neutral `sys/os/abi` ABIs.
- **Scope Certification**: This audit certifies `trg v0.3.0` as **Release-Qualified on macOS arm64**. Full multi-platform qualification certificates for `linux-x86_64` and `linux-aarch64` will be produced upon execution on Linux runners.

---

## 9. Final Decision

- **P0**: 0
- **P1**: 0
- **P2**: 1 (Recorded)
- **Recommendation**: **GO** to tag `v0.3.0` on commit `51491f03bf0dceaabdab9e5155d21f33bf49e986` and proceed with release packaging.

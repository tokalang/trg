# trg v0.3.1 Release Audit Report

- **Package**: `trg`
- **Release Version**: `0.3.1`
- **Audit Date**: `2026-09-02T04:13:00Z`
- **License**: Apache-2.0 (distributed in root `LICENSE` and included in archive)
- **Qualification Scope**: Release-Qualified on **macOS arm64 (`macos-15`)** and **Linux x64 (`ubuntu-22.04`)**; Linux arm64 pending.
- **Toolchain**: `toka 1.0.0-rc.11`, `tokac 1.0.0-rc.11 (Built: Sep 1 2026 09:45:37)`, `compiler = "1.0.0-rc.11"`
- **Host SDK SHA-256 (`macos-arm64`)**: `20e32db7246f75fb59bc452c411e59ab5df8b4ecbd3dbc4beb9b2d6e4532d07b`
- **Linux SDK SHA-256 (`linux-x64`)**: `24169a99aba87fd94f16699eadd2a7623d8862a3fe27dec06c46f95b53346917`
- **Audit Decision**: **GO (Fully Multi-Platform Release-Qualified on macOS arm64 and Linux x64)**

---

## 1. Release Purpose & Defect Ledger

`v0.3.1` is a release compliance and distribution fix release that replaces `v0.3.0` while maintaining full immutable release history:

1. **[P0 Resolved] License Compliance**: Added full Apache-2.0 `LICENSE` file to repository and confirmed inclusion in release archives.
2. **[P1 Resolved] Application Black-Box Verification**: Added automated verification script `tests/verify_application_release.py` that tests archive integrity, online dependency fetching, compilation, functional sanity, and offline rebuild replay.
3. **[P1 Resolved] Accurate Platform Scope**: Platform qualification explicitly documented as macOS arm64 and Linux x64.
4. **[P2 Documented] Build Contract**: Clear documentation that application tarballs perform online `toka fetch` on first build to generate lock, enabling deterministic `TOKA_OFFLINE=1` builds thereafter.

| Severity | Count | Status | Notes |
| :--- | :---: | :---: | :--- |
| **P0 (Blocker)** | **0** | **RESOLVED** | Root `LICENSE` present and verified in `trg-0.3.1.tar.gz`. |
| **P1 (Critical)** | **0** | **RESOLVED** | Black-box application release verifier and multi-platform CI passed. |
| **P2 (Minor)** | **1** | **RECORDED** | Human/Count modes construct `MatchRange` vector; `is_match` fast-path queued for Phase 2. |

---

## 2. Supply Chain & Public Registry Integrity

The dependency supply chain remains locked to immutable official releases:

- **Dependency**: `official/regex @ 0.3.0`
- **Release Asset**: `https://github.com/tokalang/regex/releases/download/v0.3.0/regex-0.3.0.tar.gz`
- **Asset SHA-256**: `cf365755da461ff9bb48df211aca8ddbcf2c630cc1caedd8395b5200a8d8beb9`
- **Tree SHA-256**: `c0a8b50305ae4e6e7ee82c1505ca314dd0bc98968d088d5e631dc39104db68a1`

---

## 3. Black-Box Application Consumer Verification

The verifier script [`tests/verify_application_release.py`](file:///Users/zhyi/GitDP/tokalang/trg/tests/verify_application_release.py) performs automated end-to-end qualification of the packaged release archive:

1. **Archive Security & License**: Confirms no `.git`, no binaries, no path escapes, and presence of `LICENSE`.
2. **Online Fetch**: Pulls `regex:0.3.0` from `pkg.tokalang.dev` and creates `package.lock`.
3. **Compilation & Version**: Builds `target/debug/trg` and asserts exact version identity `trg 0.3.1 (Toka)`.
4. **Functional Matrix**: Asserts `-F`, `-E -w`, `-n -C 1`, and `--json` (`trg-json-v2`).
5. **Offline Replay**: Purges unpacked packages and target directory, executing `TOKA_OFFLINE=1 toka build` with exit code 0.

---

## 4. Multi-Platform Package Qualification (49/49 PASS)

All 49 qualification tests executed cleanly across both macOS arm64 and Linux x64 with the official Toka `1.0.0-rc.11` SDK:

- **`toka doctor`**: Standard library, compiler, formatter, and linker driver OK.
- **`toka check --json package.tk`**: Diagnostics clean (`success: true`).
- **`toka build`**: Successfully built `target/debug/trg`.
- **49 Qualification Tests (`tests/qualify.py`)**: 49 / 49 PASS (100%).

---

## 5. Final Decision

- **P0**: 0
- **P1**: 0
- **P2**: 1 (Recorded)
- **Recommendation**: **GO** to tag `v0.3.1`, publish GitHub Release, and advance registry latest to `0.3.1`.
